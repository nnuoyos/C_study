제어문

1.조건문
-if문



switch(변수 or 수식){
	case 값1:
	변수, 수식 == 값1 실행할 명령
	break;// 비교를 중단
	case 값2:
	case 값3:
	default:
		모든 값의 경우가 아닐 때
		}

*변수는 1개의 값만 가질 수 있다.
*수식의 결과도 1개의 결과값만 나온다.



-switch문
	switch(변수명){
	case 값1:
		실행할 문장;
	case 값2:
		실행할 문장;
	case 값3:
		실행할 문장;
	case 값4:
		실행할 문장;
	......
	default:
		실행할 문장;
	}