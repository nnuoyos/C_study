-switch문

switch(변수 or 수식){
	case 값1:
	변수, 수식 == 값1 실행할 명령
	break;// 비교를 중단
	case 값2:
	break;
	case 값3:
	break;
	default:
	break;(c# 이나 다른 언어에서도 switch문이 사용되는데, 그 때는 디폴트 끝에도 break사용함.
	처음부터 습관 들이기 위해 c++ 에서도 디폴트 끝에 break 붙이기로 하자~!)
		모든 값의 경우가 아닐 때
		}

*변수는 1개의 값만 가질 수 있다.
*수식의 결과도 1개의 결과값만 나온다.

